# 西子灣海況警示 App — 實作規格書(給 Claude Code)

> 本文件為單一地點(西子灣)水上活動安全警示 App 的完整實作規格,整合所有前置研究與決策。請依此規格實作,若有規格未涵蓋的細節,可依既有設計原則合理推斷,或標記為待確認事項。

---

## 1. 產品概述

一個顯示西子灣「現在適不適合下水」的 App,以紅黃綠三色燈號 + 插畫波浪 + 插畫狗角色呈現,取代使用者自己解讀氣象數據的認知負擔。目標使用者是一般人/家庭/觀光客,不是專業海洋運動玩家(市場上 GoOcean 等 App 已服務專業玩家,本產品鎖定「零解讀成本」的差異化定位)。

MVP 僅服務**西子灣單一地點**,不需要定位權限、不需要地點切換功能。

---

## 2. 資料來源與 API 規格

所有資料來自中央氣象署開放資料平臺(opendata.cwa.gov.tw),需要 API 授權碼(已由使用者取得,存於環境變數/secrets,不寫死在程式碼中)。

### 2.1 潮汐預報 — F-A0021-001

```
GET https://opendata.cwa.gov.tw/fileapi/v1/opendataapi/F-A0021-001?Authorization={AUTH}&format=JSON&LocationName=高雄市鼓山區
```

⚠️ **必須帶 LocationName 參數**,否則回傳全臺灣資料,檔案巨大、查詢極慢。

回傳結構重點路徑:
```
result.records.TideForecasts[0].Location.TimePeriods.Daily[] 
  → 每筆: Date, LunarDate, TideRange("大"/"中"/"小"), Time[]
    → 每筆: DateTime(ISO,含+08:00), Tide("滿潮"/"乾潮"), 
      TideHeights.AboveChartDatum(cm,建議採用此基準,最低低潮位為0)
```

### 2.2 海況觀測 — O-B0075-001(需兩測站互補查詢)

```
GET https://opendata.cwa.gov.tw/fileapi/v1/opendataapi/O-B0075-001?Authorization={AUTH}&format=JSON&StationID=C4P01
GET https://opendata.cwa.gov.tw/fileapi/v1/opendataapi/O-B0075-001?Authorization={AUTH}&format=JSON&StationID=COMC08
```

| StationID | 測站 | 提供欄位 |
|-----------|------|---------|
| C4P01 | 高雄潮位站 | TideHeight, TideLevel, SeaTemperature |
| COMC08 | 彌陀資料浮標 | WaveHeight, WaveDirection, WavePeriod, PrimaryAnemometer.WindSpeed, PrimaryAnemometer.WindScale(蒲福風級) |

⚠️ **兩測站欄位互斥**(潮位站無浪高風速,浮標站無潮位海溫),必須分別查詢再合併,不可只查一個站。

⚠️ **最新一筆資料常為 null**:須實作「由新到舊掃描,取第一筆非 null 值」邏輯,並記錄該筆的實際時間戳記(用於顯示「更新於 X 分鐘前」)。

### 2.3 日出日沒時刻 — A-B0062-001

```
GET https://opendata.cwa.gov.tw/fileapi/v1/opendataapi/A-B0062-001?Authorization={AUTH}&format=JSON&Date=2026-07-15&CountyName=高雄市
```

⚠️ **必須帶 Date 參數**(格式 yyyy-MM-dd,查詢當天日期),否則回傳過期的固定範圍資料。

回傳重點欄位:`SunRiseTime`, `SunSetTime`(格式 HH:mm,如 "18:47")。

---

## 3. 資料整合邏輯

### 3.1 統一內部資料格式

後端程式需將上述三筆資料標準化、合併為單一 JSON:

```json
{
  "location": "西子灣",
  "lastFetchedAt": "2026-07-15T09:00:00+08:00",
  "tide": {
    "height": 159,
    "level": "漲潮",
    "tideRange": "大",
    "observedAt": "2026-07-15T07:43:00+08:00",
    "source": "C4P01 / F-A0021-001"
  },
  "wave": {
    "height": 2.2,
    "period": 6.5,
    "direction": 236,
    "observedAt": "2026-07-15T06:00:00+08:00",
    "source": "COMC08"
  },
  "wind": {
    "speed": 5.6,
    "scale": 4,
    "observedAt": "2026-07-15T06:00:00+08:00",
    "source": "COMC08"
  },
  "seaTemperature": {
    "value": 31.0,
    "observedAt": "2026-07-15T07:00:00+08:00",
    "source": "C4P01"
  },
  "sun": {
    "sunrise": "05:23",
    "sunset": "18:47"
  },
  "alertLevel": "green",
  "isOpenNow": true,
  "minutesUntilSunset": 645
}
```

### 3.2 缺值處理規則

對每個測站的觀測序列,由新到舊掃描,取第一筆該欄位非 null 的資料,並記錄其 `observedAt` 時間戳記(不可只存數值,要老實反映資料實際新舊)。

---

## 4. 紅黃綠燈判斷邏輯(定案版)

```
若符合以下任一條件 → 紅色(危險,勿下水)
  - 風級(WindScale) ≥ 6
  - 浪高(WaveHeight) > 3 公尺
  - 現在時間非開放時段(見第5節)
  [註:氣象特報資料 MVP 階段未串接,若未來取得豪雨/強風/雷雨特報資料源,應加入此條件]

否則,若符合以下任一條件 → 黃色(需注意)
  - 風級(WindScale)為 4-5
  - 浪高(WaveHeight)介於 1-2 公尺
  - 現在時間屬於潮汐交替時段(滿潮/乾潮前後,時間窗定義待確認,可先用前後1小時作為預設值)
  - 距離日落 < 2 小時

否則(風級 0-3 且浪高 ≤ 1 公尺) → 綠色(適合下水)
```

**依據**:黃/紅分界(風級6級)對應氣象署「陸上強風特報」官方門檻,非自訂數值,具公信力基礎。

### 蒲福風級對照表(供邏輯與 UI 顯示參考)

| 風級 | 風速範圍 | 海面狀態描述 | 平均浪高(m) |
|------|---------|-------------|-------------|
| 0-2 | <3.3 m/s | 平靜~小微波 | ≤0.2 |
| 3 | 3.4-5.4 m/s | 微波較大,開始有白頭浪 | 0.6 |
| 4 | 5.5-7.9 m/s | 小浪拖長,白頭浪頻密 | 1.0 |
| 5 | 8.0-10.7 m/s | 中浪,浪花飛濺 | 2.0 |
| 6 | 10.8-13.8 m/s | 大浪開始出現 | 3.0 |
| 7+ | >13.9 m/s | 海浪堆疊,高浪階段 | 4.0+ |

---

## 5. 開放時間與日落倒數邏輯

西子灣開放時間:每日 09:00 至當天日落(用 A-B0062-001 動態取得,不可寫死固定時間)。

```
若 現在時間 < 09:00           → isOpenNow = false,顯示「尚未開放」
否則若 現在時間 > 當天日落時刻   → isOpenNow = false,顯示「已過開放時間」,即使海況數字正常也顯示此狀態,並提示現場無救生員駐點
否則若 距離日落 < 2 小時        → isOpenNow = true,正常顯示紅黃綠燈,額外疊加日落倒數提示(例:「距離日落還有 1 小時 45 分鐘,若還要通勤請提早出發」)
否則                          → isOpenNow = true,正常顯示,不特別提示時間
```

---

## 6. 後端架構

```
GitHub Actions(排程,每 15-30 分鐘觸發一次)
  → 執行抓取程式(依第2節規格呼叫三個 API)
  → 執行第3節標準化與缺值處理邏輯
  → 執行第4-5節判斷邏輯,產出完整 JSON(含 alertLevel, isOpenNow, minutesUntilSunset 等衍生欄位)
  → 寫入 xiziwan-status.json
  → 透過 GitHub Pages 發布為公開靜態網址
App 端
  → 定期 fetch 該 JSON 網址,渲染畫面
  → 不直接呼叫氣象署 API,不內嵌 API 授權碼
```

授權碼存於 GitHub Secrets,不寫入版本控制或 App 內。

---

## 7. 視覺設計規格

### 7.1 分層原則
- **天空背景**:擬真渲染風格(類 iOS 內建氣象 App),依天氣現象(晴/陰/雨)切換不同影片/動態素材,**非真實照片**
- **海浪**:插畫色塊風格(非寫實水面紋理),顏色對應 alertLevel(綠/黃/紅),波動幅度與速度隨等級遞增(綠色平緩慢速 → 紅色劇烈快速)
- **狗角色**:插畫/線條風格,浮於海浪與天空交界線,隨波浪動態同步位移;三種情緒版本對應三個等級(開心/警覺/緊張)

### 7.2 畫面資訊(無地名文字,因單一地點)
- 溫度、天氣現象文字/圖示
- alertLevel 對應的底部行動呼籲文字(隨等級變化語氣,例:Enjoy your play / Be aware of your surroundings / Do not enter the water)
- 資料最後更新時間(誠實顯示,若非即時需標明)
- 非開放時段/日落倒數提示(依第5節邏輯疊加顯示)

### 7.3 無障礙(色盲/色弱友善)
三色等級除了顏色本身,**必須**額外搭配形狀標示(不可僅靠顏色辨識),例如:綠色配圓形/勾勾圖示、黃色配三角形/驚嘆號圖示、紅色配八角形/叉叉圖示。

### 7.4 需要的畫面/狀態
1. 主畫面(三種 alertLevel × 開放時段內)
2. 非開放時段畫面(尚未開放 / 已過開放時間)
3. 資料無法取得的錯誤/離線狀態(需誠實顯示,不可顯示錯誤顏色或黑屏)
4. 詳細海況頁(潮汐、浪高、風速、海溫等原始數據攤開顯示)
5. 「關於本App」頁(完整免責聲明、資料來源標示、隱私政策、官方連結)
6. 首次開啟引導頁(精簡版重點提醒)

---

## 8. 視覺素材清單(由使用者提供,已備妥/準備中)

- 天氣圖示組:Sunny, Cloudy, Overcast, Rainy, Heavy Rain, Thunderstorm, Windy, Hazy, Shower, Sunrise, Sunset, Precipitation, Visibility, Feel Like, Humidity, Averages(已有,藍色線條風格)
- 待補:潮汐圖示(漲/退潮)、浪高圖示、蒲福風級量表視覺、海溫專用圖示(與氣溫圖示區隔)
- 狗角色三態插畫(開心/警覺/緊張)
- 色盲友善形狀標示三組
- 天空背景動態素材(晴/陰/雨,直式 9:16 比例,seamless loop)
- App 圖示(1024×1024px)、載入畫面、錯誤狀態插畫、非開放時段圖示

實作時若素材尚未交付,請先用色塊/預留位置(placeholder)搭建畫面結構,待素材到位後替換,不要因等待素材而阻塞其他功能開發。

---

## 9. 法律文字(可直接嵌入使用)

### 9.1 資料來源顯名聲明(必須,法律要求)
```
本 App 使用以下中央氣象署開放資料:
・交通部中央氣象署 [當年年份] 潮汐預報(F-A0021-001)
・交通部中央氣象署 [當年年份] 海象監測資料-48小時浮標站與潮位站海況監測資料(O-B0075-001)
・交通部中央氣象署 [當年年份] 日出日沒時刻資料(A-B0062-001)

此開放資料依政府資料開放授權條款(Open Government Data License)進行公眾釋出,
使用者於遵守本條款各項規定之前提下,得利用之。
政府資料開放授權條款:https://data.gov.tw/license
```
年份建議動態抓取當下年份,不寫死。

### 9.2 首次開啟引導頁文字
```
【出發前,你需要知道】

本 App 的海況燈號,是根據中央氣象署開放資料自動估算,
幫助你快速判斷「今天適不適合去西子灣玩水」——
但這是輔助參考,不是最終答案,以下幾件事務必留意:

・西子灣沙灘開放時間:每日上午9時至當天日落
  (非開放時段沒有救生員駐點,請避免此時段下水)
・本 App 顏色僅供參考,可能與現場實際狀況不同,
  請以現場公告及實際海況為準
・海象資料可能有延遲或短暫中斷,App 會標示資料
  「最後更新時間」,請留意數據是否夠新
・下水前建議先評估自身泳技與體能狀況

如遇緊急狀況請立即撥打 119(消防/救護)或 118(海巡署)
```

### 9.3「關於本App」完整版文字(含官方連結與免責條款)
```
【重要提醒(完整版)】

中央氣象署(包括其僱員及代理人)對於開放資料之準確性、可用性、
完整性或效用,不作任何明確或暗示的保證。使用本 App 所生之任何
損失、損害或傷害,開發者與資料提供機關均不負賠償或補償責任。

【更多官方資訊】
・中央氣象署西子灣天氣頁面:https://www.cwa.gov.tw/V8/C/L/Beach/Beach.html?PID=A008
・高雄市政府海洋局水域遊憩活動公告:https://kcmb.kcg.gov.tw/cp.aspx?n=C27F055F98DA8C5B
・國立中山大學西子灣沙灘開放須知:https://property-oga.nsysu.edu.tw/var/file/102/1102/img/540654205.pdf

如遇緊急狀況請立即撥打 119(消防/救護)或 118(海巡署)
```

### 9.4 隱私政策(無定位權限版本)
```
【隱私政策】

1. 定位資訊:本 App 目前僅提供西子灣單一地點之海況資訊,不需要、
   也不會取用您的裝置定位權限。
2. 資料蒐集:本 App 不要求註冊帳號,不主動蒐集您的個人身分資訊。
3. 第三方資料:本 App 顯示之海況資訊來自中央氣象署公開資料,
   非使用者個人資料。
4. 聯絡方式:若對隱私權有任何疑問,請透過 [聯絡email] 與我們聯繫。

最後更新日期:[上架前填入日期]
```
需發布至公開網址(如 GitHub Pages)供 App Store Connect 後台填寫。

---

## 10. 尚未決定/待確認事項(不阻塞開發,但需留意)

- 潮汐交替時段的具體時間窗定義(目前判斷邏輯先預設滿潮/乾潮前後1小時,可日後調整)
- 是否/如何聯繫高雄市政府海洋局做閾值校準(非必要,屬加分項目)
- 氣象特報(豪雨/強風/雷雨)資料源尚未串接,MVP 階段第4節判斷邏輯未包含此條件,建議後續補上
- 非開放時段是否完全隱藏海況數字,或繼續顯示但加註提示(留待視覺設計階段決定)
- 商業模式(免費/廣告/付費)、App 正式名稱、Apple Developer 帳號等上架行政作業尚未處理,不影響開發階段

---

## 附錄:已完成之互動原型邏輯參考

已驗證的核心互動概念(供動畫實作參考):波浪路徑用 sine 疊加曲線產生,波幅與週期依 alertLevel 調整(綠色低幅慢速、紅色高幅快速),狗角色座標跟隨波浪路徑上該 x 座標的 y 值即時定位,使其視覺上「浮」在浪的交界線上。
